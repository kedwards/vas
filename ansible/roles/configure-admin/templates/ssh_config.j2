{% for host in groups['swarm_managers'] + groups['swarm_workers'] + groups['proxy_nodes'] %}
Host {{ host }}
  Hostname {{ host }}
  User docker
  StrictHostKeyChecking no
  UserKnownHostsFile /dev/null

{% endfor %}
